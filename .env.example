# Neon PostgreSQL connection string (asyncpg driver for the app)
# Use the pooled connection string from your Neon project for production
DATABASE_URL=postgresql+asyncpg://user:password@host/dbname?sslmode=require
